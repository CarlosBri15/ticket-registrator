/** Default number of items per page in paginated lists. */
export const PAGE_SIZE = 10;

/** Default number of completed reports to show on the dashboard before the
 *  "show more" toggle expands the full history. */
export const HISTORY_LIMIT = 5;
